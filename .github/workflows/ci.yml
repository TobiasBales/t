name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  ignite:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Ignite
      run: ./ignite.sh $(pwd)
